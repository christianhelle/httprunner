[package]
name = "httprunner"
version = "0.1.0"
edition = "2024"
authors = ["Christian Helle"]
description = "HTTP File Runner - Execute HTTP requests from .http files"
license = "MIT"
repository = "https://github.com/christianhelle/httprunner"
homepage = "https://github.com/christianhelle/httprunner"
documentation = "https://github.com/christianhelle/httprunner"
readme = "src/README.md"
keywords = ["http", "rest", "api", "testing", "cli"]
categories = [
  "command-line-utilities",
  "development-tools",
  "web-programming::http-client",
]

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1.0"
clap = { version = "4.5", features = ["derive"] }
walkdir = "2.5"
rand = "0.9.2"
uuid = { version = "1.19", features = ["v4", "fast-rng", "macro-diagnostics"] }
regex = "1.12.2"
base64 = "0.22.1"

[dev-dependencies]
tempfile = "3"

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
