# Test IntelliJ HTTP Client syntax handling

# Request with IntelliJ assertion block
POST https://httpbin.org/post
Content-Type: application/json

{
  "test": "data",
  "value": 123
}

> {%
    client.test("Request executed successfully", function() {
        client.assert(response.status === 200, "Response status is not 200");
    });
%}

###

# Another request with multi-line IntelliJ script
GET https://httpbin.org/json

> {%
    client.test("Response is valid JSON", function() {
        client.assert(response.body.hasOwnProperty('slideshow'), "Missing slideshow property");
        client.log("Response processed");
    });
%}

###

# Request without IntelliJ syntax (should work as before)
GET https://httpbin.org/uuid

EXPECTED_RESPONSE_STATUS 200
EXPECTED_RESPONSE_BODY "uuid"
